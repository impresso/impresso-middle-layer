{
  "type": "object",
  "description": "Content item audio timestamp",
  "title": "Content Item Audio Timestamp",
  "additionalProperties": false,
  "properties": {
    "timeCode": {
      "type": "array",
      "minLength": 2,
      "maxLength": 2,
      "description": "Array of time codes representing the start offset and length of the audio segment in seconds.",
      "items": {
        "type": "number",
        "description": "Time code in seconds"
      }
    },
    "textLocation": {
      "type": "array",
      "minLength": 2,
      "maxLength": 2,
      "description": "Array of text locations representing the character ofset and length of the audio segment in the content item text.",
      "items": {
        "type": "integer",
        "description": "Text location in characters"
      }
    }
  }
}
