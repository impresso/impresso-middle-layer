{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Year",
  "description": "A year (TODO)",
  "additionalProperties": false,
  "required": ["y"],
  "properties": {
    "uid": {
      "type": "integer",
      "description": "Numeric representation of the year"
    },
    "values": {
      "description": "Weights values (TODO)",
      "$ref": "./YearWeights.json"
    },
    "refs": {
      "description": "Weights references (TODO)",
      "$ref": "./YearWeights.json"
    }
  }
}
