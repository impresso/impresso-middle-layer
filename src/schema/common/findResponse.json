{
  "$id": "https://github.com/impresso/impresso-middle-layer/tree/master/src/schema/common/findResponse.json",
  "type": "object",
  "title": "Default Response",
  "additionalProperties": false,
  "required": ["data", "limit", "skip", "total", "info"],
  "properties": {
    "data": {
      "type": "array",
      "items": {}
    },
    "limit": {
      "type": "integer",
      "description": "The number of items returned in this response"
    },
    "skip": {
      "type": "integer",
      "description": "The number of items skipped in this response"
    },
    "total": {
      "type": "integer",
      "description": "The total number of items matching the query"
    },
    "info": {
      "type": "object",
      "description": "Additional information about the find response.",
      "properties": {}
    }
  }
}
