{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Content Item",
  "description": "A journal/magazine content item (article, advertisement, etc.)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "uid": {
      "type": "string",
      "description": "The unique identifier of the content item.",
      "minLength": 1
    },
    "copyrightStatus": {
      "type": "string",
      "description": "Copyright status.",
      "enum": ["pbl", "und", "nkn", "euo", "unk", "in_cpy"]
    },
    "type": {
      "type": "string",
      "description": "The type of the content item, as present in the OLR provided by the data provider. All content items are not characterised by the same set of types."
    },
    "sourceMedium": {
      "type": "string",
      "description": "Medium of the source (audio for audio radio broadcasts, print for newspapers, typescript for digitised radio bulletin typescripts).",
      "enum": ["audio", "print", "typescript"]
    },
    "title": {
      "type": "string",
      "description": "The title of the content item."
    },
    "transcript": {
      "type": "string",
      "description": "Transcript of the content item."
    },
    "locationEntities": {
      "type": "array",
      "description": "Linked location entities mentioned in the content item.",
      "items": {
        "$ref": "./NamedEntity.json"
      }
    },
    "personEntities": {
      "type": "array",
      "description": "Linked person entities mentioned in the content item.",
      "items": {
        "$ref": "./NamedEntity.json"
      }
    },
    "organisationEntities": {
      "type": "array",
      "description": "Linked organisation entities mentioned in the content item.",
      "items": {
        "$ref": "./NamedEntity.json"
      }
    },
    "newsAgenciesEntities": {
      "type": "array",
      "description": "Linked news agency entities mentioned in the content item.",
      "items": {
        "$ref": "./NamedEntity.json"
      }
    },
    "topics": {
      "type": "array",
      "description": "Topics mentioned in the content item.",
      "items": {
        "$ref": "./TopicMention.json"
      }
    },
    "embeddings": {
      "type": "array",
      "description": "Precomputed embeddings for the content item in the format: <model_type>:<base64_embedding_vector>.",
      "items": {
        "type": "string"
      }
    },
    "transcriptLength": {
      "type": "number",
      "description": "The length of the transcript in characters.",
      "minimum": 0
    },
    "totalPages": {
      "type": "number",
      "description": "Total number of pages the item covers.",
      "minimum": 0
    },
    "languageCode": {
      "type": "string",
      "description": "ISO 639-1 language code of the content item."
    },
    "isOnFrontPage": {
      "type": "boolean",
      "description": "Whether the content item is on the front page of the publication."
    },
    "publicationDate": {
      "type": "string",
      "description": "The publication date of the content item.",
      "format": "date-time"
    },
    "issueUid": {
      "type": "string",
      "description": "Unique issue identifier"
    },
    "countryCode": {
      "type": "string",
      "description": "ISO 3166-1 alpha-2 country code of the content item."
    },
    "providerCode": {
      "type": "string",
      "description": "The code of the data provider."
    },
    "mediaUid": {
      "type": "string",
      "description": "Media title alias. Usually a 3 letter code of the media title (newspaper, radio station, etc.)."
    },
    "mediaType": {
      "type": "string",
      "description": "The type of the media the content item belongs to.",
      "enum": ["newspaper", "radio_broadcast", "radio_magazine", "radio_schedule", "monograph", "encyclopedia"]
    },
    "hasOLR": {
      "type": "boolean",
      "description": "Whether the content item has OCR/OLR data available."
    },
    "ocrQualityScore": {
      "type": "number",
      "description": "OCR quality score of the content item (0 - 1).",
      "minimum": 0,
      "maximum": 1
    },
    "relevanceScore": {
      "type": "number",
      "description": "Relevance score of the content item (0 - 1).",
      "minimum": 0,
      "maximum": 1
    },
    "pageNumbers": {
      "type": "array",
      "description": "Page numbers the content item appears on.",
      "items": {
        "type": "number",
        "minimum": 1
      }
    }
  },
  "required": ["uid"]
}
