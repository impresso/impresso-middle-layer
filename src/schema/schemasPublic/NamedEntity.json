{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An named entity (location, persion, etc) present in text.",
  "type": "object",
  "title": "Named Entity",
  "additionalProperties": false,
  "properties": {
    "uid": {
      "type": "string",
      "description": "Unique identifier of the entity"
    },
    "count": {
      "type": "number",
      "description": "How many times it is mentioned in the text"
    }
  },
  "required": ["uid"]
}
