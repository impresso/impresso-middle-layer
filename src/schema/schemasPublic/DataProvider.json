{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DataProvider",
  "description": "A data provider is a partner institution that provides content to Impresso (e.g., libraries, archives, media organizations).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique identifier of the data provider."
    },
    "names": {
      "type": "array",
      "description": "Names of the data provider in different languages.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "langCode": {
            "type": "string",
            "description": "ISO 639-1 language code."
          },
          "name": {
            "type": "string",
            "description": "Name of the data provider in this language."
          }
        },
        "required": ["langCode", "name"]
      }
    }
  },
  "required": ["id", "names"]
}
