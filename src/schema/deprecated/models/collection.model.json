{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "CollectionModel",
  "description": "Description of the collection object (Collection class)",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "description", "status", "creationDate", "lastModifiedDate", "countItems", "creator"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 2,
      "maxLength": 50
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "maxLength": 50
    },
    "description": {
      "type": "string",
      "maxLength": 500
    },
    "status": {
      "type": "string",
      "minLength": 2,
      "maxLength": 3,
      "title": "Status of the collection",
      "examples": ["PRI", "DEL"]
    },
    "creationDate": {
      "type": "string"
    },
    "lastModifiedDate": {
      "type": "string"
    },
    "countItems": {
      "type": ["integer", "string"],
      "title": "Number of items in the collection",
      "default": 0,
      "examples": [3245]
    },
    "creator": {
      "$ref": "./base-user.model.json"
    },
    "labels": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
