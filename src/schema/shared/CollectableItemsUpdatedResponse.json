{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Request to update collectible items in a collection",
  "title": "CollectableItemsUpdatedResponse",
  "type": "object",
  "required": ["totalAdded", "totalRemoved"],
  "properties": {
    "totalAdded": {
      "type": "integer",
      "description": "Total number of items added to the collection"
    },
    "totalRemoved": {
      "type": "integer",
      "description": "Total number of items removed from the collection"
    }
  }
}
