{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Impresso Text Embedding Request",
  "description": "Body of a request to the Impresso Text Embedding endpoint",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "searchTarget": {
      "type": "string",
      "description": "Where the embedding is going to be used",
      "enum": ["image", "text"]
    },
    "text": {
      "type": "string",
      "description": "Text to be embedded",
      "maxLength": 8000
    }
  },
  "required": ["searchTarget", "text"]
}
